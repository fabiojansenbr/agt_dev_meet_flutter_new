import 'package:flutter/material.dart';

class UsingFlutterWorld extends StatelessWidget {
  const UsingFlutterWorld({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset(
        'assets/images/using_flutter_world.png',
      ),
    );
  }
}
